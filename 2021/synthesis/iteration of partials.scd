(
x = Array.newClear(5);
[6,4,0.5,10,7].do{ arg item, count;
	x[count] = item.squared;}
)

z = [6,4,0.5,10,7].collect(_.squared)

5.do{arg item; item.postln};

//////////////////////////////////////////////////////
//////////////////////////////////////////////////////
//////////////////////////////////////////////////////

(SynthDef.new(\iter, { var temp,sum; sum=0;

	10.do{ temp = VarSaw.ar( 40 * {Rand(0.99,1.02)}!2,{Rand(0.0,1.0)}!2,0.05);
	sum = sum +temp;
};
sum = sum *0.05;
Out.ar(0,sum);
}).add;
)

s = Synth.new(\iter);


////////////////////////////////////////////////////
/////////////////////////////////////////////
////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////
(SynthDef.new(\iter, {
	arg freq =80;
	var temp,sum;
	sum=0;

	10.do{ arg count;
		temp = SinOsc.ar(freq*(count +1)*LFNoise1.kr({Rand(0.05,0.2)}!2).range(0.98,1.02));
		///amp flactuation
		temp = temp* LFNoise1.kr({Rand(0.5,8)}!2).exprange(0.01,1);
	sum = sum +temp;
};
sum = sum *0.05;
Out.ar(0,sum);
}).add;
)

s = Synth.new(\iter);



